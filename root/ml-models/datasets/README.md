# Datasets

Lista de arquivos em `datasets/` (exemplos):

- `OsteoporosisPredictionInCKD.csv`
- `OsteoporosisRiskPrediction.csv`
- pastas: `okx_datasets/`, `opckd_datasets/`, `orp_datasets/` com versões pré-processadas e splits de treino/teste.

Uso

- Examine os notebooks correspondentes em `notebooks/` para ver como cada CSV é carregado e pré-processado.
- Não versionar datasets grandes no Git; usar links ou instruções de download quando aplicável.

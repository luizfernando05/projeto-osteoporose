# Frontend (`app`)

Descrição

Aplicação frontend construída com React / Expo (ver `package.json`). Interface para entrada de dados e visualização de resultados.

Como rodar

```
cd app
npm install
npm run start
```
